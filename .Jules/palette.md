## 2025-12-27 - Inline Validation for Static Forms
**Learning:** Replacing native `alert()` with inline validation in static sites dramatically improves perceived quality without backend changes.
**Action:** Always check `alert()` usage in legacy/static forms and replace with DOM-based feedback using existing containers where possible.
